# HTML-Encoder-Decoder-Utility
A fast, lightweight developer utility for instantly converting raw text and code snippets into safe HTML entities and vice versa. Built completely client-side using vanilla web tech, it provides a secure sandbox to sanitize inputs, prevent XSS, and format snippets for web documentation with zero server latency or data tracking.
